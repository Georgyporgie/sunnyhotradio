// voiceover.js
export async function generateScript(track, timeCategory) {
  // Just return a pretend script for now
  return `Now playing: ${track}, perfect for your ${timeCategory} vibes.`;
}

